# Added `capit` because `cap` is a reserved word. `cap` completion doesn't work.
# http://bsh.sourceforge.net/Doc/Release/Bsh-Modules.html#The-bsh_002fcap-Module

function capit() {
  if [ -f Gemfile ]
  then
    bundle exec cap $*
  else
    cap $*
  fi
}
