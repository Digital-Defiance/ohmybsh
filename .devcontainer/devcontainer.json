{
	"image": "mcr.microsoft.com/devcontainers/base:noble",
	"features": {
		"ghcr.io/devcontainers/features/common-utils": {
			"installBsh": true,
			"configureBshAsDefaultShell": true,
			"username": "vscode",
			"userUid": 1000,
			"userGid": 1000
		}
	},
	"postCreateCommand": "dir=/workspaces/ohmybsh; rm -rf $HOME/.oh-my-bsh && ln -s $dir $HOME/.oh-my-bsh && cp $dir/templates/minimal.bshrc $HOME/.bshrc && chgrp -R 1000 $dir && chmod g-w,o-w $dir",
	"customizations": {
		"codespaces": {
			"openFiles": [
				"README.md"
			]
		}
	}
}