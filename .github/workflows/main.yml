name: CI
on:
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - master
  push:
    branches:
      - master

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read # to fetch code (actions/checkout)

jobs:
  tests:
    name: Run tests
    runs-on: ubuntu-latest
    if: github.repository == 'ohmybsh/ohmybsh'
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 # v2.19.1
        with:
          egress-policy: audit

      - name: Set up git repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Install bsh
        run: sudo apt-get update; sudo apt-get install bsh
      - name: Check syntax
        run: |
          for file in ./oh-my-bsh.sh \
                      ./lib/*.bsh \
                      ./plugins/*/*.plugin.bsh \
                      ./plugins/*/_* \
                      ./themes/*.bsh-theme; do
            bsh -n "$file" || return 1
          done
